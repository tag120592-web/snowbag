A linear progress indicator — `<TNProgressBar>`.

```jsx
<ProgressBar value={64} label="Анализ документа" showValue />
<ProgressBar indeterminate />
```

Props: `value`/`max`, `tone`, `size`, `label`, `showValue`, `indeterminate`.
