A radio button — `<TNRadio>`. Group several with a shared `name`.

```jsx
<Radio name="scope" label="Весь документ" defaultChecked />
<Radio name="scope" label="Только выбранный раздел" />
```

Props: `checked`, `name`, `value`, `label`, `description`, `disabled`, `onChange`.
