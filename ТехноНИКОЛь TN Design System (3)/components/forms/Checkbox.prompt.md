A checkbox with optional label and description — `<TNCheckbox>`. Checked state fills brand red.

```jsx
<Checkbox label="Учитывать отменённые редакции" />
<Checkbox indeterminate label="Выбрать все" />
```

Props: `checked`, `defaultChecked`, `indeterminate`, `label`, `description`, `disabled`, `onChange`.
