An on/off toggle — `<TNTumbler>`. Red track when on.

```jsx
<Switch label="Показывать только критичные" defaultChecked />
```

Props: `checked`, `defaultChecked`, `label`, `size` (sm/md), `disabled`, `onChange`.
