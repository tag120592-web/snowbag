A count or dot notification marker.

```jsx
<Badge count={12} />
<Badge dot tone="green" />
<Badge count={5}><Icon name="bell" /></Badge>
```

Props: `count`, `dot`, `tone`, `max`, optional `children` to anchor it to a target.
