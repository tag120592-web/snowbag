The base surface container — `<TNCard>`. White, 12px radius, hairline border.

```jsx
<Card padding="lg" hoverable>
  <h3>Аудит проекта «Кровля-2024»</h3>
</Card>
```

Props: `padding` (none/sm/md/lg or px), `elevation` (none/small/large), `bordered`, `radius`, `hoverable`.
